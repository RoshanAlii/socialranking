Validation completed
====================

JavaScript test suite: 29 passed, 0 failed
Independent Python cross-check: matched the JavaScript audit for 233 observed posts,
29 confirmed public profiles, highest supported interactions, highest likes,
highest comments, highest public video views, and zero publicly reported shares
JavaScript syntax checks: provider, normalize, rank, ingest, validator and dashboard script passed

Formula checked:
postsPerWeek = postsInWindow * 7 / 30

Live limitation:
The existing repository snapshot is based on the profile scraper's 12-post payload and cannot be accurately recalculated. The new workflow must run once with APIFY_TOKEN to create the correct values for all profiles. Until that succeeds, the updated dashboard hides old cadence values rather than showing estimates.

New answer coverage:
Per-profile analytics, content records, format medians, metric coverage, and
leaderboards are stored in the snapshot and rejected by the validator if they
do not exactly match a full recomputation from normalized raw captures.
